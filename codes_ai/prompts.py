
def _prompt_save(sector):
    return f"""
    Extensive markdown information of Cookie DAO. 
    
    Requirements:
        - Focus only on collected information (do not halucinate)
        - For each element provide required information (do not halucinate).
    """